import Box from '@mui/material/Box';

export default function Head (props) {
    const sx = {
        height: 53,
        borderBottom: '1px solid #bbb',
        padding: '11px 22px',
    };

    return (
        <Box sx={sx}>
        </Box>
    );
}
