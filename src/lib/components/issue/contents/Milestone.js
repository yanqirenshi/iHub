import React from 'react';

import Box from '@mui/material/Box';

const style ={
    background: 'rgba(235, 246, 247, 1.0)',
    width: '100%',
    height: '100%',
};

export default function Comments (props) {
    return (
        <Box sx={style}>
        </Box>
    );
}
