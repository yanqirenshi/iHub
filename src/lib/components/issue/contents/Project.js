import React from 'react';

import Box from '@mui/material/Box';

const style ={
    background: 'rgba(219, 208, 230, 1.0)',
    width: '100%',
    height: '100%',
};

export default function Project (props) {
    return (
        <Box sx={style}>
        </Box>
    );
}
