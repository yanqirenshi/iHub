import React from 'react';

import Box from '@mui/material/Box';

const style ={
    background: 'rgba(214, 233, 202, 1.0)',
    width: '100%',
    height: '100%',
};

export default function PertGraph (props) {
    return (
        <Box sx={style}>
        </Box>
    );
}
